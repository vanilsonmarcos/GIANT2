const config = { 
    db: {
        host:"localhost",
        user:"root",
        password: "agoraoununca",
        database: "giant_db",
        connectTimeout: 60000,

    },
    listPerPage: 10,
}

export default config;