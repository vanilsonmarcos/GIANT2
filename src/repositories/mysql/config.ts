const config = { 
    db: {
        host:"localhost",
        user:"root",
        password: "root",
        database: "Giant",
        connectTimeout: 60000,

    },
    listPerPage: 10,
}

export default config;