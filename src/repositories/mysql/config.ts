const config = { 
    db: {
        host:"localhost",
        user:"root",
        password: "root",
        database: "Giant_DB",
        connectTimeout: 60000,

    },
    listPerPage: 10,
}

export default config;